import { FacturesearchPipe } from './facturesearch.pipe';

describe('FacturesearchPipe', () => {
  it('create an instance', () => {
    const pipe = new FacturesearchPipe();
    expect(pipe).toBeTruthy();
  });
});
